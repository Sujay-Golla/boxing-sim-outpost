#pragma once

// Arduino IDE requires every source file for a sketch to live beneath the
// sketch's same-named folder. Keep the shared implementation in Firmware while
// exposing it to this sketch through this forwarding header.
#include "../config.h"
